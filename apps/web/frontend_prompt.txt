1. Currently Multi tenant - Multi role school application is implementing ...
2. We have created the replit demo for front end implmentation
3. But the forms and flow and look and feel are very ordinary in this generation
4. As per AI with best features available:
 4.1 Need to implment a new repo from scractch by taking referance of each module referance.
 4.2 Use common utility for Date fields
 4.3 Use Common for Date rane and form fields [reusable components]
 4.4 Common Non field Error processings and Form Field Error processings [Heighlight that form field- if we have any error in response from backend]
 4.5 Common Toast messeges with new look and field and user feel better experience
 4.6 Common Filter utility through out the application
 4.7 Common Data Table for list pages to show data 
 4.8 Pagination for Data tables
 4.9 Better Page look up with Profile Images and Organization Images in the dash board with latest floating view
 4.10 Proper profile sections 
 4.11 Finally Keep components reusable for all roles [Admin/Teacher/Student(Parent)]
 4.12 Use latest tailwind or ano better framework for Type script and implement the application
 4.13 Follows the ESlint and typescript thing .. Make code matches that .
 4.14 PERFORMANCE: Render header ONCE in ProtectedLayout wrapper (not per page) - fetch user/org data once
 4.15 MOBILE: Hide logo on mobile, use gradient header with glass-morphism, responsive sizing (h-16 sm:h-20)
 4.16 LAYOUT: Header width - Logo section (w-64 fixed), Organization (natural), User controls (ml-auto right)
 4.17 FORMS: Create generic field components supporting multiple types (string, number, bool, choice, multi-choice)
 4.18 FORMS: Use key prop on Select components to force re-render for view/edit modes: key={`${itemId}-${field.value}`}
 4.19 VALIDATION: Extract validation to utils folder (don't inline in components) - make it reusable
 4.20 GROUPING: Group settings/preferences by category, use Tabs for navigation with icons
 4.21 BORDERS: Add border-bottom to each field (px-4 py-4 last:border-b-0) not wrapper divs
 4.22 TYPOGRAPHY: Use default Label component sizing (text-sm) - don't override. Hierarchy: text-2xl → text-lg → text-sm
 4.23 RESPONSIVE: Grid layouts grid-cols-1 sm:grid-cols-2 lg:grid-cols-3. Headers flex-col sm:flex-row
 4.24 SIDEBAR: Mobile sidebar fixed with transform translate-x, hamburger at top-4 left-4 (not floating bottom)
 4.25 TYPES: NO any types - use proper interfaces. Error type is Error not any. Icons Record<string, string>
 4.26 CLEANUP: Remove unused imports, NO console statements (ESLint will error), run eslint --fix before commit
 4.27 API: Match backend serializer structure exactly. Handle nested objects. Invalidate related queries after mutations
 4.28 EXPORTS: Create index.ts barrel exports in each folder for clean imports from single source
 4.30 TESTING: Pre-commit - 0 ESLint errors, 0 warnings allowed, TypeScript compiles, Prettier applied
 4.31 REUSABLE COMPONENTS: Create PageHeader component with smart icon mapping based on title keywords
 4.32 ICON MAPPING: Auto-detect icons from page titles (Holiday→Calendar, Leave→Briefcase, Settings→Gear)
 4.33 PAGE HEADERS: Consistent typography (text-3xl bold), optional icon with gradient container, action buttons
 4.34 CHILDREN POSITIONING: PageHeader children (buttons/dialogs) render on right side with flex-shrink-0
 4.35 DELETE DIALOGS: Reusable DeleteConfirmationDialog with orange/amber theme (industry standard warning colors)
 4.36 SOFT DELETE: Support isSoftDelete prop with context-aware messaging ("archived/restorable" vs "permanent")
 4.37 CALENDAR THEMES: Use distinct color schemes (blue/indigo for calendar) to avoid visual conflicts with page
 4.38 TOAST NOTIFICATIONS: Add closeButton prop with circular X button positioned top-right (right: 12px, top: 12px)
 4.39 SPACING CONSISTENCY: Remove component margins, let parent containers control spacing with space-y-* utilities
 4.40 BULK UPLOAD: Detect top-level error field in API responses, display file-level errors at row 0
 4.41 CALENDAR FEATURES: Auto-detect weekend patterns, generate weekend holidays, combine with API holidays
 4.42 DATE HIGHLIGHTS: Show today's date with blue circle indicator, handle overlap with holidays properly
 4.43 HOLIDAY TYPES: Color-coded legend (Festival=Orange, Organization=Green), visual differentiation in calendar
 4.44 CALENDAR NAVIGATION: Month navigation (Previous/Today/Next), view toggle (Calendar/Table), date range display
 4.45 DUAL VIEW MODES: Calendar grid view with month layout, Table view with sortable columns and filters
 4.46 WORKING DAY POLICY: Respect organization settings (Sunday off, Saturday patterns: Full/First/Second/Third/Off)
 4.47 HOLIDAY DIALOGS: Click date to view/add/delete holidays, styled with gradient headers and decorative elements
 4.48 FORM VALIDATION: Client-side validation with visual feedback, server-side error handling with field highlighting
 4.49 RESPONSIVE CALENDAR: Mobile-optimized (hide weekday names on small screens, stack navigation controls)
 4.50 BARREL EXPORTS: Export components from index.ts for clean imports (import { PageHeader } from '@/components/common')
 4.51 ALWAYS keeps mutations in separate file better modularity 
 4.52 Use PageHeader for Headers for Forms for uniqueness
 4.53 ESLINT STRICT: no-console rule set to ERROR (not warn), only console.warn/error allowed, --max-warnings=0 enforced
 4.54 CODE QUALITY: Zero tolerance for console.log/info/debug statements, all removed from codebase
 4.54: Keep mobile view as well into considerations for look and feel

On front end side regarding the folder strucutre ..what ever the code I have done is as party of the Admin role ...

But now we need to reuse the same code or create new peave of logic to show the side bars or links or pages will go based on roles and their permissions ..

We will ahve three 3 roles as of now ..Admin[Pricipal] Employee [Staff of schoool] and Parent ..

So keep this in mind and create the folder structure preoperly which explains the role so later point of the time it will easy for debug and also keep common components for all roles ..

Same goes with backend keep folders in api [admin, employee and parent ] so corresponding apis will go to hte corresponding folders and add the preoper authorization and authentication classses



Expo:
-----
Create a Web aplication along with Andriod and IOS applications ..

Permissions handling, like , camera , notifications, locations with a fallback if user deny those permissions.
Navigation structure like going from one from tab to another or going back by clicking on back button.
API handling, loading state, errors, retries etc.
Network handling, if you app requires internet, then how you handle the offline network.
Responsive UI, your app should be responsive for all the android and ios devices.
Push notifications 
Performance handling, it should avoid unnecessary reloads


You have to be certain you removed all of the side effects required to make that package work. 


What is a Privacy manifest?
A privacy manifest is a file named PrivacyInfo.xcprivacy that is included in your iOS native project. This file is used to declare why the app includes native code that calls into certain APIs that Apple considers sensitive.


Make sure you have updated your Expo SDK libraries to the latest versions for your SDK version using npx expo install —fix.

You can include an iOS privacy manifest in a bare Expo app by creating a PrivacyInfo.xcprivacy file using Xcode and adding it to your iOS app target. Follow Apple's Privacy manifest files guide to create a PrivacyInfo.xcprivacy file.


When developing a native app that requires access to potentially sensitive information on a user's device, such as their location or contacts, the app must request the user's permission first. For example, to access the user's media library, the app will need to run MediaLibrary.requestPermissionsAsync().


app.json

Copy

{
  "android": {
    "permissions": ["android.permission.SCHEDULE_EXACT_ALARM"]
  }
}


app.json

Copy

{
  "android": {
    "blockedPermissions": ["android.permission.RECORD_AUDIO"]
  }
}





app.json

Copy

{
  "ios": {
    "infoPlist": {
      "NSCameraUsageDescription": "This app uses the camera to scan barcodes on event tickets."
    }
  }
}


Resetting permissions
Often you want to be able to test what happens when a user rejects permissions, to ensure your app reacts gracefully. An operating-system level restriction on both Android and iOS prohibits an app from asking for the same permission more than once (you can imagine how this could be annoying for the user to be repeatedly prompted for permissions after rejecting them). 

Create a .env file in the root of your project directory and add environment-specific variables on new lines in the form of EXPO_PUBLIC_[NAME]=VALUE:

.env

Copy

EXPO_PUBLIC_API_URL=https://staging.example.com
EXPO_PUBLIC_API_KEY=abc123
Now you can use environment variables directly in your source code:
import { Button } from 'react-native';

function Post() {
  const apiUrl = process.env.EXPO_PUBLIC_API_URL;

  async function onPress() {
    await fetch(apiUrl, { ... })
  }

  return <Button onPress={onPress} title="Post" />;
}


How to read from environment variables
 Every environment variable must be statically referenced as a property of process.env using JavaScript's dot notation for it to be inlined. For example, the expression process.env.EXPO_PUBLIC_KEY is valid and will be inlined.
 Alternative versions of the expression are not supported. For example, process.env['EXPO_PUBLIC_KEY'] or const {EXPO_PUBLIC_X} = process.env is invalid and will not be inlined.

Linking …


Reusable Components:
--------------------
If your screen using any of the 
1. DatePicker - Through same date picker and Datetiepicker reusable component everywhere()
2. Multi select searchable dropdown ()
3. Searchble Dropdown ()
4. Errormessege wrappers ()
5. KeyboardAwareScrollView -- Use for all Mobile Screens for Form entries
  





for better typescript & react production project ..what prompt do we need to give for pilot
To get production-ready TypeScript and React code from GitHub Copilot, you should move beyond simple "build a list" prompts and instead use structured instructions that enforce strict typing, accessibility, and modern patterns. 

ecanarys.com
 +1
1. The "Senior Engineer" Component Prompt
Use this prompt to ensure your components are scalable and meet production standards. 
Prompt: "You are an expert React and TypeScript engineer. Create a fully typed functional component for [describe feature].
Rules: Use React 19 and TypeScript strict mode. Define props with interfaces.
Requirements: Include ARIA labels for accessibility, handle loading/error states, and use Tailwind CSS for styling.
Standard: Export as default, add JSDoc with @example, and include a simple unit test using Vitest/React Testing Library."
 

Medium
·Stefano Alvares
 +4
2. Robust Custom Hook Prompt
Production projects rely on well-isolated logic. Use this to avoid common "stale closure" bugs. 

Medium
·Amandeep Kour
Prompt: "Write a custom React hook in TypeScript named use[Name] for [purpose].
Rules: Follow all 'Rules of Hooks' and ensure a proper dependency array for useEffect.
Output: Return a typed object containing { data, loading, error, refetch }. Include JSDoc for each return value."
 

Medium
·Amandeep Kour
 +1
3. Systematic Refactoring Prompt
For improving existing codebases, use a prompt that targets specific production bottlenecks. 

Medium
·Amandeep Kour
Prompt: "Analyze this React component for performance issues.
Focus: Identify unnecessary re-renders, optimize expensive calculations with useMemo, and ensure all third-party library types are correctly imported.
Task: Refactor the code to be more readable and performant while maintaining identical functionality."
 

Medium
·Amandeep Kour
 +4
Best Practices for Copilot in Production
Use Custom Instructions: Create a .github/custom-instructions.md file (or use /.prompt.md) to define project-wide standards like your preferred styling library or naming conventions.
Keep Context Open: Copilot performs better when relevant files (like your theme provider or global types) are open in active tabs.
Iterate with Agents: Use the /init command or "Agent mode" in GitHub Copilot in VS Code to scaffold entire project structures rather than single files.
Setup Commands: For a quick, modern project start, ask: "How to initiate a TypeScript Vite application with Tailwind CSS and React using npm?". 

YouTube
·Alex Rusin
 +7





20 sites
The Complete GitHub Copilot Prompt Strategy
Prompts are requests made to GitHub Copilot and can include questions in chat, code comments for autocompletion, or specific instr...

1m

YouTube
·
Alex Rusin
Building a landing page with Copilot agent mode
After you give it a prompt, agent mode streamlines complex coding tasks by autonomously iterating on its own code, identifying and...


The GitHub Blog
How to write better prompts for GitHub Copilot
An example where you can try your hand at prompting GitHub Copilot to assist you in building a browser extension. * Progress over ...


The GitHub Blog
Show all
Give everything in once
To get optimal, production-grade results from GitHub Copilot instantly, paste this master system prompt into your Copilot Chat settings, your custom instructions, or directly at the start of your project chat session.
The Ultimate Master Prompt
text
Act as a Principal React and TypeScript Architect. Every line of code you write must be production-ready, highly secure, scalable, and optimized for performance.

Follow these strict technical standards for all generations:
1. Framework & Language: Use React 19 and TypeScript 5+ in strict mode. Use functional components with arrow syntax. Explicitly type all component Props, event handlers, and API responses using interfaces (no 'any').
2. Performance: Prevent unnecessary re-renders. Use 'useCallback' and 'useMemo' only where computationally justified. Implement lazy loading for heavy sub-components.
3. State Management: Keep state local where possible. Use custom hooks to decouple business logic from the UI. Ensure 'useEffect' cleanups are handled perfectly to prevent memory leaks.
4. UI & Accessibility: Use semantic HTML elements. Implement proper ARIA attributes, focus states, and keyboard navigation. Use Tailwind CSS for mobile-first, utility-based styling.
5. Error Handling: Always include loading, error, and empty states. Wrap asynchronous operations in try/catch blocks with typed error responses.
6. Documentation & Testing: Provide inline JSDoc for complex logic. Always include a companion Vitest and React Testing Library unit test suite covering success, failure, and edge cases.

Apply these rules directly to the following request: [INSERT YOUR TASK HERE]
Use code with caution.
3 Quick Steps to Automate This
Create a Configuration File: Save this text inside a .github/copilot-instructions.md file in your repository root so Copilot applies it to every query automatically.
Keep Core Files Open: Open your global.d.ts, tailwind.config.js, or API client files in active IDE tabs so Copilot references your exact project context.
Use Slash Commands: Combine this master prompt with commands like /explain to audit existing files, or /tests to instantly generate the robust test suites demanded above.
I can help you build your project starter. If you want, tell me:
What is the specific feature or component you need to build right now?
Which state management tool do you use (Zustand, Redux Toolkit, Context API)?
Do you need an API fetching layer setup (Axios, TanStack Query)?




is that prompt fine for better code reusability , efficeincey and hook managements and statemanagements ..in react and react native with type screipt?
The master prompt provided earlier is a solid baseline, but it has two critical gaps that will fail you in production: it relies on older boilerplate patterns that ignore React 19's native data features, and it overlooks the severe performance bottlenecks unique to React Native.
To maximize code reusability, hook efficiency, and dual-platform state management, you need a specialized prompt that addresses these cross-platform nuances.
The Problem With the Previous Prompt
React 19 Obsolescence: The previous prompt still asks for manual try/catch and manual loading/error state juggling. React 19 completely automates this via Async Actions, the use hook, and useActionState.
React Native Invisible Crashes: React Native handles computational re-renders much worse than web viewports. Heavy list rendering or improper state triggers in atomic items can drop mobile application frames to zero.
State Drift: It doesn't explicitly force the decoupling of UI from data frameworks, which is mandatory if you intend to share hooks between React (Web) and React Native (Mobile). 

Telerik.com
 +2





3 sites
React Native Interview Prep: Redux, Performance, and Tradeoffs
19 Feb 2026 — Ritesh Singh. Frontend Developer ★ React Native Developer ★ React Js ★ TypeScript ★ JavaScript. 2mo. Spent the last few weeks deep...


LinkedIn
·
Ritesh Singh
The Guide to New Hooks in React 19 - Telerik.com
21 Oct 2024 — React 19 has recently emerged as a powerful evolution in the React ecosystem, bringing with it a suite of new React Hooks designed...


Telerik.com
React 19 `use` Hook Deep Dive — Using Promises Directly in ...
19 Aug 2025 — When React 19 introduced the use hook, it quietly changed one of the oldest patterns in React — the way we deal with async data. N...


DEV Community
Give me one more time
Here is the updated, ultimate master prompt optimized specifically to handle both React (Web) and React Native (Mobile).
It explicitly enforces advanced code reusability, efficient hook patterns, and clean state management.
The Ultimate React & React Native Master Prompt
text
Act as a Principal Architect specializing in cross-platform React, React Native, and TypeScript. Every line of code you write must be production-ready, highly reusable, efficient, and scale beautifully.

Adhere strictly to these architectural standards:

1. Code Reusability & Architecture:
   - Separate UI from logic. Extract all stateful and side-effect logic into standalone custom hooks.
   - Design components using the Single Responsibility Principle. Build highly modular, atomic components.
   - For web/mobile parity, ensure business logic (hooks/state) is decoupled from platform-specific UI (HTML vs. View/Text).

2. Advanced Hook Management:
   - Write custom hooks that return structured, predictable states: { data, isLoading, error, actions }.
   - Prevent memory leaks: always return cleanup functions in 'useEffect'.
   - Avoid stale closures: properly declare all dependencies in 'useEffect', 'useCallback', and 'useMemo'.
   - Memoize callback handlers passed to deeply nested child components using 'useCallback'.

3. State Management Standards:
   - Default to local or component-coallocated state. Use Context API *only* for low-frequency global updates (e.g., themes, auth).
   - For high-frequency or complex global states, default to a lightweight external store pattern (e.g., Zustand style) with selector-based access to prevent unnecessary re-renders.
   - Keep global actions isolated from state objects to maximize performance.

4. Platform & Language Execution:
   - Language: TypeScript 5+ in strict mode. Explicitly type all Props, custom hook returns, and API contracts. Zero 'any' types allowed.
   - React (Web): Use React 19 standards, semantic HTML, and Tailwind CSS. Ensure strict ARIA compliance.
   - React Native (Mobile): Use standard core components (View, Text, Pressable, StyleSheet). Use 'FlashList' or 'FlatList' with 'keyExtractor' and memoized 'renderItem' for long lists. Ensure layout performance via 'StyleSheet.create'.

5. Robustness & Testing:
   - Gracefully handle loading, error, and empty states. Wrap async calls in try/catch blocks with strongly typed errors.
   - Provide clean JSDoc for custom hooks. Include a companion unit test using Vitest / React Testing Library (for Web) or React Native Testing Library (for Mobile).


Checklist For front end Implementation:
————————————————————
Use Below reusable components where ever is possible in the new code implementations
Multi Select dropdown
Date Component
Searchble Drop Component
Error Wrapper
Data Table components
Reusable Filter component
Use separate layers for each flow:
Define Hooks separately
Define interfaces separately
Define shared code for Mob and web gracefully