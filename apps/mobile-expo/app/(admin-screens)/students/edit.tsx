/**
 * Edit Student Screen (Admin)
 * Re-exports the shared screen to avoid code duplication.
 */
import EditStudentScreen from '../../(shared-screens)/students/edit';

export default EditStudentScreen;
