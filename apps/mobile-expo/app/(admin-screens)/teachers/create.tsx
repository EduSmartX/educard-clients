/**
 * Create Teacher Screen (Admin)
 * Re-exports the shared screen to avoid code duplication.
 */
import CreateTeacherScreen from '../../(shared-screens)/teachers/create';

export default CreateTeacherScreen;
