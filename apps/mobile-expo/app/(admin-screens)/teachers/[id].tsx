/**
 * Teacher Detail Screen (Admin)
 * Re-exports the shared screen to avoid code duplication.
 */
import TeacherDetailScreen from '../../(shared-screens)/teachers/[id]';

export default TeacherDetailScreen;
