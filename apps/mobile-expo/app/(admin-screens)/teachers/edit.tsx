/**
 * Edit Teacher Screen (Admin)
 * Re-exports the shared screen to avoid code duplication.
 */
import EditTeacherScreen from '../../(shared-screens)/teachers/edit';

export default EditTeacherScreen;
