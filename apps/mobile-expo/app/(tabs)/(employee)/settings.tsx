/**
 * Employee Settings Screen
 * Re-exports the admin settings screen which is role-aware
 * (shows Organization section only for admins).
 */
import SettingsScreen from '../(admin)/settings';

export default SettingsScreen;
