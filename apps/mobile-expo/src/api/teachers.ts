/**
 * Teachers API — Re-export shim
 * @deprecated Import from '@/features/teachers' instead
 */
export {
  getTeachers,
  getTeacherById,
  createTeacher,
  deleteTeacher,
  restoreTeacher,
  type TeacherQueryParams,
  type TeacherListResponse,
} from '@/features/teachers';
