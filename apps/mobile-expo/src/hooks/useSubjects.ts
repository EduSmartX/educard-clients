/**
 * Subjects Hooks — Re-export shim
 * @deprecated Import from '@/features/subjects' instead
 */
export {
  subjectKeys,
  useSubjects,
  useSubjectsByClass,
  useSubjectDetail,
  useCreateSubject,
  useDeleteSubject,
} from '@/features/subjects';

export type { SubjectQueryParams } from '@/features/subjects';
